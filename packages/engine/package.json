{
  "name": "@cinder/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cinder 通訊引擎：ChatBackend 契約＋實作（真實 relay／記憶體 demo）、本機儲存抽象、換機/搬家/快照——與 UI 無關，供任意前端重用（ADR-0074）",
  "license": "AGPL-3.0-only",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cinder/core": "workspace:*",
    "@cinder/relay": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
